# Examples

- `api_request_examples.jsonl` shows host-to-router request objects for protocol 6.3.
- `api_response_examples.jsonl` shows representative structured router outputs.
- `python_router/` is an optional direct-API command-line reference implementation.

Example confidence values are illustrative and should not be interpreted as calibrated probabilities.
